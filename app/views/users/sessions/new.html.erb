<%= stylesheet_link_tag 'login' %>
<div class="login-background">
  <div class="row h-100">
    <div class="col-7 d-none d-lg-block hero-image"></div>
    <div class="col">
      <div class="login-box mx-auto">
        <div class="logo"></div>
        <div>
          <label>Sign in using your credentials.</label>

          <fieldset class="TableLike small-12">
            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <%= devise_error_messages! %>

              <div class="field">
                <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "someone@example.com" %>
              </div>
              <div class="Clear"></div>

              <div class="field">
                <%= f.password_field :password, autocomplete: "current-password", placeholder: "password" %>
              </div>

              <% if devise_mapping.rememberable? -%>
                <div class="field">
                  <%= f.check_box :remember_me %>
                  <%= f.label :remember_me %>
                </div>
              <% end -%>

              <div class="actions">
                <%= f.submit "Sign in", :class => 'button' %>
              </div>
              <div class="Clear"></div>
            <% end %>
          </fieldset>

          <%= render "devise/shared/links" %>

        </div>
        <div class="footer">
          <label>Please feel free to give us your feedback about this tool!</label>
        </div>
      </div>
    </div>
  </div>
</div>
